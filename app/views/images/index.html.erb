<h1>Image Library</h1>

<%= link_to 'New image', new_image_path %>

<h2>Images</h2>
<table>
  <% @images.each do |image| %>
    <tr>
      <td><% image.id %></td>
      <td><%= image_tag image.url, class: "index_image" %></td>
    </tr>
  <% end %>
</table>
